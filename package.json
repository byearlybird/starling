{
	"name": "@byearlybird/starling-repo",
	"version": "0.0.0",
	"type": "module",
	"private": true,
	"workspaces": [
		"packages/**"
	],
	"scripts": {
		"build": "bun run build:core && bun run build:plugins-poll-sync && bun run build:plugins-query && bun run build:plugins-unstorage",
		"build:core": "cd packages/core && bun run build.ts",
		"build:plugins-poll-sync": "cd packages/plugins/poll-sync && bun run build.ts",
		"build:plugins-query": "cd packages/plugins/query && bun run build.ts",
		"build:plugins-unstorage": "cd packages/plugins/unstorage && bun run build.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "2.2.5",
		"@types/bun": "latest",
		"mitata": "^1.0.34",
		"tinybench": "^5.0.1",
		"tsdown": "^0.15.9",
		"typescript": "^5"
	}
}
